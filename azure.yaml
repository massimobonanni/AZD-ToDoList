# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: tdd-azd-todolist
metadata:
  template: tdd-azd-todolist@1.0.0

infra:
  provider: "bicep"

services:
  web:
    project: ./src/web
    language: dotnet
    host: appservice

  api:
    project: ./src/api
    language: dotnet
    host: function
